<meta charset="{{ site.charset }}">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{ post.title ? post.title ~ ' — ' ~ site.name : site.name }}</title>
{{ function('wp_head') }}
